server {
    listen 80;
    server_name bookkeeping.holic.work;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/acme;
        default_type text/plain;
    }

    location / {
        return 503;
    }
}
